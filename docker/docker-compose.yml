name: anythingllm

networks:
  anything-llm:
    driver: bridge

services:
  # 🎯 Qdrant向量数据库服务
  # 无原生依赖,通过HTTP API连接,兼容所有CPU架构
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333"  # HTTP API
      - "6334:6334"  # gRPC (可选)
    volumes:
      - "../qdrant-data:/qdrant/storage"  # 数据持久化
    environment:
      - QDRANT__LOG_LEVEL=INFO
      # 可选: 启用API密钥保护
      # - QDRANT__SERVICE__API_KEY=${QDRANT_API_KEY}
    networks:
      - anything-llm
    restart: unless-stopped
    # healthcheck暂时移除，因为Qdrant镜像中没有curl/wget
    # Qdrant服务启动后会自动监听6333端口，可通过日志确认

  anything-llm:
    container_name: anythingllm
    platform: linux/amd64
    # 使用本地构建而不是远程镜像，以包含最新的代码修复
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        ARG_UID: ${UID:-1000}
        ARG_GID: ${GID:-1000}
    # image: azu12138/promptx-agenticrag:latest
    cap_add:
      - SYS_ADMIN
    volumes:
      - "./.env:/app/server/.env"
      - "../server/storage:/app/server/storage"
      - "../collector/hotdir/:/app/collector/hotdir"
      - "../collector/outputs/:/app/collector/outputs"
      - "./mcp_servers.json:/app/server/storage/plugins/anythingllm_mcp_servers.json"
      # ✅ PromptX数据持久化 - 整个.promptx目录
      - "./.promptx:/root/.promptx"
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "3001:3001"
    env_file:
      - .env
    networks:
      - anything-llm
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # ✅ 依赖Qdrant服务先启动
    depends_on:
      - qdrant
    restart: unless-stopped

  # ❌ 删除：不再需要独立的PromptX容器
  # PromptX现在通过NPX在主应用容器内启动
  # promptx:
  #   container_name: promptx
  #   image: deepracticexs/promptx:latest
  #   ports:
  #     - "5203:5203"
  #   volumes:
  #     - "./.promptx:/root/.promptx"
  #   networks:
  #     - anything-llm
  #   restart: unless-stopped
  #   environment:
  #     - NODE_ENV=production
